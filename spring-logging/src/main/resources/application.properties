# This name is the service column in every log line.
spring.application.name=spring-logging
server.port=8080
logging.level.com.example.springlogging=INFO
logging.level.genesis.common.logging=INFO

# Body/run-node sample. Turn on and list fields for header-based services.
genesis.logging.header.enabled=false
